using UnityEngine;

public class StartScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("StartScript is running!");
    }

    // Update is called once per frame
    void Update()
    {
        // Lägg din logik här
    }
}
